micronaut.application.name=end2end-tests

# Default URLs for the containerised microservices (override via env vars in CI)
iot.base-url=${IOT_BASE_URL:`http://localhost:8080`}
reactive.base-url=${REACTIVE_BASE_URL:`http://localhost:8081`}
